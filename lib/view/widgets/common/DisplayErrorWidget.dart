import 'package:flutter/material.dart';

class DisplayErrorWidget extends StatelessWidget {
  DisplayErrorWidget({required Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Erreur !"),
    );
  }
}
